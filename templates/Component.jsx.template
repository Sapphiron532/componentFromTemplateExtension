import React from 'react';
import bem from 'bem-cn';

import './{filename}.css';

const cn = bem('{filenameLowerCase}');

export default function {filename}(props) {
    return (
        <div className={cn()}>
                
        </div>
    );
}
